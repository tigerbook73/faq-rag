services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: faq
      POSTGRES_PASSWORD: faq
      POSTGRES_DB: faq
    ports:
      - "5432:5432"
    volumes:
      - faq_pg_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  faq_pg_data:
